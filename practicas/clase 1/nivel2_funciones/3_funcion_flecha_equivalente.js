/* Reescribe la función anterior (multiplicar) usando arrow function. */
//creacion de la funcion anonima, como solo tiene un argumento en el cuerpo de la funcion no van llaves
const multiplicar1 = (a,b) => a*b

//llamado de la funcion anonima
console.log(`la multiplicacionde 7*6 es: ${multiplicar1(7,6)}`)